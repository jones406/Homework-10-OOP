
//In addition to Employee's properties and methods, Manager will also have officeNumber, getRole()
class Manager extends Employee {
    constructor(name, id, email)
    super(officeNumber)
    getRole() //needs to be overridden to return 'Manager'
}